# Results

Generated plots are written here by:

```bash
uv run mirage plot --runs runs/ --output-dir results/
```

Committed image artifacts should be treated as small local sanity runs, not benchmark claims.
