seed: 7
backend: numpy
device: auto
run_name: smoke
steps_per_cycle: 80
episodes_per_eval: 1
world_model_steps: 2
actor_critic_steps: 2
batch_size: 4
sequence_length: 8
imagination_horizon: 6
replay_capacity: 4000
warmup_steps: 40
max_episode_steps: 120
render_size: 96
video_fps: 12
d_model: 64
latent_dim: 32
hidden_dim: 96
learning_rate: 0.0003
